import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    mainColor: "#005E53",
    headerColor: "#00B267",
    primaryButtonColor: "#F1E500",
    errorMessageColor: "#FF3F3F",
  },
});

export default theme;
